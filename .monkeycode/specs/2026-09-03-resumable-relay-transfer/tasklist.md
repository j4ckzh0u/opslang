# 需求实施计划

- [x] 1. 实现恢复传输元数据基础层
  - [x] 1.1 在 `pkg/ops-core-sdk/file/resume.go` 定义部分文件路径、元数据模型、校验规则、过期判断和原子元数据写入
  - [x] 1.2 为元数据空输入、损坏 JSON、源哈希变化、超大偏移、空文件和过期边界编写单元测试
  - [x] 1.3 验证设计正确性属性 1、2、3 和 7：原子最终文件、恢复前缀安全、最终完整性、会话隔离

- [x] 2. 实现 SFTP 偏移传输原语
  - [x] 2.1 在 `internal/sshx/sftp.go` 增加远程 stat、指定偏移上传、指定偏移下载、前缀读取和原子 rename
  - [x] 2.2 确保所有原语传播 `context.Context` 取消并关闭文件与 SFTP 资源
  - [x] 2.3 使用内存 SFTP server 测试零偏移、非零偏移、目标过大、空文件、取消和 rename 失败

- [x] 3. 接入可恢复分发与收集
  - [x] 3.1 扩展 `DistributeOptions`、`CollectOptions` 和每主机结果字段，并保持默认直接 SFTP 行为
  - [x] 3.2 在 `ssh_transfer.go` 实现远端内容寻址、部分文件恢复、完整校验和原子替换
  - [x] 3.3 在 `collect.go` 实现本地部分文件恢复、完整校验和原子替换
  - [x] 3.4 在解释器桥接层解析 `resume` 和部分文件保留期，并对类型和范围返回明确错误
  - [x] 3.5 编写 SDK 单元测试和真实 SFTP 集成测试，覆盖中断重试、哈希不匹配、最终文件保护和 skipped 结果

- [x] 4. 检查点：验证恢复传输增量
  - [x] 4.1 运行文件 SDK、SSH/SFTP、解释器桥接测试和 `go vet ./...`
  - [x] 4.2 确保所有测试通过,如有疑问请询问用户

- [x] 5. 实现确定性拓扑计划器
  - [x] 5.1 扩展 `DistributeTarget` 的 `relay_group` 和 tags 元数据，定义中继计划模型
  - [x] 5.2 实现显式目标字段、`relay_group` 标签、IPv4 `/24`、IPv6 `/64` 和解析失败直传的优先级
  - [x] 5.3 实现组内至少 20 台启用、每个中继最多 100 台、稳定候选排序和子组拆分
  - [x] 5.4 编写表驱动测试与属性测试，验证设计正确性属性 4、5 和 6

- [x] 6. 实现临时 HTTPS 中继服务
  - [x] 6.1 实现单文件、随机令牌、证书指纹固定、Range 请求、过期时间和并发限制
  - [x] 6.2 实现会话停止和临时资源清理，清理错误进入 warning
  - [x] 6.3 编写令牌缺失、令牌错误、路径越界、方法限制、证书指纹错误、过期和 Range 边界测试

- [x] 7. 接入真实中继分发链路
  - [x] 7.1 扩展 runner 命令入口，支持有界的 relay serve 和 relay fetch 子命令
  - [x] 7.2 使用现有 SSH 上传中继文件并下发 HTTPS 会话参数，避免向中继传递目标 SSH 凭据
  - [x] 7.3 实现中继候选切换和候选耗尽后的控制端直接 SFTP 回退
  - [x] 7.4 汇总每台主机的传输来源、恢复字节数、线传输字节数、校验和与 warning
  - [x] 7.5 编写真实 SSH/SFTP 与本地 HTTPS 的端到端测试，覆盖成功、候选失败和直接回退

- [x] 8. 扩展规模与故障模拟测试
  - [x] 8.1 扩展虚拟集群记录部分文件、Range 字节和中继组主体出口字节
  - [x] 8.2 在 1000 和 10000 主机测试中验证结果守恒、成功率、重试上界和中继流量上界
  - [x] 8.3 覆盖 0.1% 确定性故障、哈希损坏、中继失效和恢复偏移异常

- [x] 9. 更新接口文档和能力状态
  - [x] 9.1 更新标准库、架构、CLI、README 和状态报告中的新增选项、协议、安全边界与限制
  - [x] 9.2 更新或生成原子操作索引，并运行 `make docs-check`

- [ ] 10. 最终检查点：验证完整功能
  - [x] 10.1 运行 `fix` skill、全量测试、`go vet ./...`、规模测试和 `make build-all`
  - [x] 10.2 确保所有测试通过,如有疑问请询问用户
  - [ ] 10.3 提交并推送代码，跟踪 GitHub Actions 至成功
