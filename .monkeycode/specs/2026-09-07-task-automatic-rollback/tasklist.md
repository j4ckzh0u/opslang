# 需求实施计划

- [x] 1. 扩展任务 AST 与解析语法
  - [x] 1.1 为 `ast.TaskStatement` 增加可选 `Rescue` 和 `Always` 块，并更新字符串表示
    - 保持现有任务主体和目标子句兼容
    - 覆盖 Requirement 1.1、1.2、1.4
  - [x] 1.2 扩展 `parseTaskStatement` 解析有序的 `rescue` 和 `always` 子句
    - 保留词法位置并拒绝非法子句顺序
    - 覆盖 Requirement 1.1、1.2、1.3
  - [x] 1.3 编写 AST 和 Parser 单元测试
    - 覆盖仅主体、rescue、always、完整子句、逆序和孤立子句
    - 断言非法输入的行列信息
    - 覆盖 Requirement 1.1 至 1.4

- [x] 2. 扩展静态遍历、安全检查与执行模式判断
  - [x] 2.1 更新模块链接、权限检查和审批操作收集以遍历任务全部阶段
    - 主体、补偿和收尾操作使用同一脚本权限
    - 覆盖 Requirement 5.1、5.2
  - [x] 2.2 更新模式选择和强制模式验证
    - 自动模式选择可精确执行任务补偿语义的引擎
    - 强制模式无法表达语义时在连接主机前报错
    - 覆盖 Requirement 4.3、4.4
  - [x] 2.3 编写安全遍历和模式选择测试
    - 覆盖 rescue 与 always 中的变更操作和只读权限拒绝
    - 覆盖 Requirement 4.3、4.4、5.1、5.2

- [x] 3. 检查点 - 确保所有测试通过,如有疑问请询问用户

- [x] 4. 生成分阶段 Runner 指令包
  - [x] 4.1 增加块级独立指令包生成入口
    - 每个阶段重置变量状态并继承 dry-run 与 privilege
    - 覆盖 Requirement 4.1、5.1、6.1
  - [x] 4.2 将 `deployStep` 扩展为 main、rescue 和 always 包
    - 为阶段包设置稳定任务 ID 后缀并逐包校验
    - 覆盖 Requirement 2.1、3.1、4.1
  - [x] 4.3 为每个阶段包独立注入远程配置并签名
    - 签名发生在 dry-run 和远程配置确定之后
    - 覆盖 Requirement 5.3、5.4
  - [x] 4.4 编写指令包生成单元测试和属性测试
    - 验证阶段变量隔离、标识唯一、签名有效和空块边界
    - 属性 6：所有阶段包独立通过权限与签名验证
    - 覆盖 Requirement 4.1、5.3、5.4、6.1

- [x] 5. 实现逐主机补偿调度与结果聚合
  - [x] 5.1 增加部署主机的 main、rescue、always 分阶段结果模型
    - 实现 success、rolled_back、rollback_failed、cleanup_failed 状态归约
    - 覆盖 Requirement 2.3、2.4、3.2、6.2
  - [x] 5.2 在 Runner 部署循环中仅对失败主机执行 rescue
    - 主操作包执行一次，补偿包执行一次
    - 覆盖 Requirement 2.1、2.2、2.3、7.2、7.3
  - [x] 5.3 在所有已开始主机上执行 always 并停止后续任务
    - 保留上下文取消和阶段失败信息
    - 覆盖 Requirement 2.5、3.1 至 3.3、6.4
  - [x] 5.4 将分阶段结果接入部署输出和审计载荷
    - 输出触发原因、阶段包标识、时间和终态
    - 覆盖 Requirement 6.2、6.3
  - [x] 5.5 编写调度单元测试和集成测试
    - 覆盖全成功、部分失败、补偿失败、清理失败、空目标和取消
    - 属性 1：每个任务目标恰有一个 main 结果
    - 属性 2：rescue 结果仅属于 main 失败主机
    - 属性 3：每台主机每任务至多一次 rescue
    - 属性 4：每台主机每任务至多一次 always
    - 属性 5：存在 main 失败时后续任务零次启动
    - 属性 8：终态可从阶段结果确定性推导
    - 覆盖 Requirement 2、3、6、7

- [x] 6. 实现 AOT 任务补偿语义
  - [x] 6.1 将任务主体、rescue 和 always 编译为统一错误边界
    - 复用块级错误捕获语义并保持 `_error` 可见范围
    - 覆盖 Requirement 4.2
  - [x] 6.2 保持 AOT 任务目标路由的现有前置校验
    - 带动态目标的 AOT 部署在连接主机前返回明确错误
    - 覆盖 Requirement 4.4
  - [x] 6.3 编写 AOT 代码生成和执行测试
    - 覆盖主操作成功、主操作失败、rescue 失败和 always 失败
    - 属性 7：dry-run 下所有阶段执行零变更操作
    - 覆盖 Requirement 4.2、6.1

- [x] 7. 检查点 - 确保所有测试通过,如有疑问请询问用户

- [x] 8. 同步语言和部署文档
  - [x] 8.1 更新语言参考、CLI、架构、状态报告和示例
    - 说明失败主机范围、停止策略、重试边界和结构化输出
    - 覆盖 Requirement 1 至 7
  - [x] 8.2 增加示例脚本自动解析与执行验证
    - 覆盖任务级 rescue 和 always 示例
    - 覆盖 Requirement 1、4、6

- [x] 9. 完成全量质量门禁
  - [x] 9.1 运行格式、测试、静态检查、文档检查和跨平台构建
    - 执行 `gofmt`、`go test ./...`、`go vet ./...`、`make docs-check`、`make build-all` 和 `git diff --check`
    - 覆盖全部需求和正确性属性
